<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 xmlns:local="clr-namespace:Sample;assembly=Sample"
			 x:Class="Sample.GeofencePage">
	<ContentPage.Content>
		<StackLayout BackgroundColor="White">
			<StackLayout Orientation="Horizontal" Margin="10" HorizontalOptions="FillAndExpand" BackgroundColor="White">
				<Label HorizontalOptions="FillAndExpand" Margin="10">Status</Label>
				<Button x:Name="Status" Margin="0" BackgroundColor="White">Status</Button>
			</StackLayout>
			<local:CustomMap x:Name="CustomMap" MapType="Street" WidthRequest="{x:Static local:App.ScreenWidth}" HeightRequest="{x:Static local:App.ScreenHeight}" />
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
